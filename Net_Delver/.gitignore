# Godot 4 editor cache and imported resources. Regenerated on open; never commit.
.godot/
/android/

# Exported builds
/build/
*.exe
*.pck
*.zip

# export_presets.cfg is deliberately NOT ignored — the roadmap ships a Linux
# preset for the Steam Deck, and that belongs in the repo. Re-check before
# adding an Android preset, which can carry keystore credentials.
