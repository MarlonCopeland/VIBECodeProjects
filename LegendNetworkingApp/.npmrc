# Make package.json "engines" an actual gate rather than a suggestion: npm
# ignores it by default, which is how installs kept succeeding on a Node that
# cannot run this project's test suite.
engine-strict=true
